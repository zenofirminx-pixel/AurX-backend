import db from "../initMemory.js";

export default async function handler(req, res) {
  res.setHeader("Access-Control-Allow-Origin", "https://dashboard-for-aurx.vercel.app");
  res.status(200).json({ ok: true, msg: "users.js marche" });
}